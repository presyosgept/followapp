# from .models import Notification

# def notifications(request):
#     if request.user.is_authenticated:
#         return {'notifications': notifications.objects.filter(is_read=False)}
#     else:
#         return {'notifications': []}